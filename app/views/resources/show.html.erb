<div class="max-box">
        
        <%content_for :left do%>
	<span class="span3">
		 <%=render :partial=> "application/resource_left"%>
	</span>
<%end%>

        
        	<div class="form-box21">
            <% @resources.each do |resource| %>
           <div class="information-new22">

                  <div class="heading2"><%=resource.resource_type.slug.titlecase %></div>
                   <div class="technology"><%=resource.title.titlecase%></div>
                    <p><%=raw(resource.description)%><span class="read-more"><a href="#"> Read more...</a></span>
                    </p>
             </div>
              <% end %>
              <div class="clear"></div>
  
                
            </div>
            

        </div>

